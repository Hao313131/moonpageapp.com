# MoonPage搜索可见性与性能强化

> **日期**：2026-09-16 ｜ **站点**：moonpageapp.com ｜ **仓库**：`Hao313131/moonpageapp.com` ｜ **最新部署提交**：`1cf300f`
>
> **一句话结论**：五项里 **4 项已完成**。代码侧 **3 批**全部上线（`c859ab9` → `9b39974` → `1cf300f`）；GitHub 侧 **Enforce HTTPS 由我调 API 开启并复验**；**GSC 验证你已完成**（2026-09-16 确认「您是经过验证的所有者」）。仅剩 **Cloudflare 前置** 需要你的账号 + 改域名 NS。

## 一、五项状态总表

| 项 | 完成情况 | 责任方 | 关键说明 |
|---|---|---|---|
| 1. 站内评价体系 | ✅ 已完成 | 我 | **endpoint 已接通**（零账号）+ **响应判定已容错**；<br>只差你点一封激活邮件 |
| 2. Enforce HTTPS | ✅ 已完成 | 我（已用 API 替你开启） | `http→https` 已实测 301 |
| 3. 季节性 hub | ✅ 已完成 | 我 | 新增 `/winter-bedtime-stories`；<br>有真实内容兜底，非薄内容 |
| 4. GSC 验证 | ✅ 已完成 | 你（2026-09-16 确认） | 已是「经过验证的所有者」；<br>**meta 标签路线不需要执行** |
| 5. Cloudflare 前置 | ❌ 我做不到 | **你** | 需要你的 Cloudflare 账号；<br>且要改域名注册商的 NS |

## 二、我替你做了什么（本轮全部已上线）

### 2.1 Enforce HTTPS —— 我替你开启了

修复前：`http://moonpageapp.com/` 返回 **200**，不跳 https。
我用仓库凭据调用 GitHub Pages API，确认 **TLS 证书已 `approved`**（覆盖 apex + www，有效期到 2026-10-24），于是把它打开：

```
PUT /repos/Hao313131/moonpageapp.com/pages   {"https_enforced": true}
→ 读回：https_enforced = True
```

实测结果：

| 请求 | 结果 |
|---|---|
| `http://moonpageapp.com/` | **301 → https** |
| `http://www.moonpageapp.com/` | **301 → https://moonpageapp.com/** |
| `https://moonpageapp.com/` | 200 |

### 2.2 季节性 hub `/winter-bedtime-stories`

只建这一条，**因为只有它真有内容兜底**。数据核查结论：

| 判断 | 依据 |
|---|---|
| 建 winter hub | `snow` tag 有 4 本真实故事；<br>另有 `holiday-routine-disruption`、<br>`daylight-saving-and-toddler-sleep` 两篇真实季节指南 |
| 不建圣诞/万圣节 hub | 全站**没有任何** Christmas / Halloween / Easter 内容；<br>建了就是薄内容，违反项目规则 |

已接入：sitemap（158 → **159**）、全站 Footer、主 hub「More hubs」、`hubJsonLd` 的 `ItemList`。
去孤结果：被 **159 / 164 个页面**链接。

### 2.3 站内评价体系（静态站、无后端）

| 文件 | 作用 |
|---|---|
| `data/reviews.json` | 评价存储（**当前为空**） |
| `lib/reviews.ts` | 读取 + 聚合；<br>**count=0 时不输出任何评分标记** |
| `components/ReviewSection.tsx` | 故事页**可见**评价区；<br>Google 要求标记的评分必须可见 |
| `components/ReviewForm.tsx` | 提交表单；<br>未配 endpoint 时自动降级为 mailto |
| `scripts/ingest-reviews.mjs` | `npm run reviews:ingest` 把提交合并进 store |
| 故事页 `Book` 节点 | 条件输出 `aggregateRating` + `review` |
| 首页 `MobileApplication` 节点 | 条件输出站点总分 |

**端到端验证过**（随后已完全回滚）：

| 验证点 | 实测 |
|---|---|
| 故事页 `Book.aggregateRating` | `{"ratingValue":"5.0","reviewCount":1}` + 1 个 `Review` 节点 |
| 页面可见星级 | `5.0 out of 5` 可见 |
| 首页 `MobileApplication.aggregateRating` | `{"ratingValue":"4.0","reviewCount":1}` |
| 回滚后线上 `aggregateRating` | **0**（`grep -rl aggregateRating out/` = 0） |

> ⛔ **绝不用 App Store 评分**：Google 明文 *"Don't aggregate reviews or ratings from other websites."* —— 跨站聚合有手动处罚风险。

### 2.4 全途径排名

| 途径 | 现状 |
|---|---|
| OG / Twitter 卡 | 已在位（`pageMetadata` 全站输出） |
| Breadcrumb / FAQPage / HowTo / Article | 已在位 |
| WebSite + SearchAction（站点搜索框） | 已在位 |
| RSS feed / sitemap / hreflang / IndexNow | 已在位 |
| **Speakable（语音搜索）** | **本轮新增**：新 hub + 全部 64 篇指南（共 66 页） |
| **星级富结果** | **本轮新增**：评价体系（见 2.3） |

### 2.5 CWV（LCP / INP / CLS）

| 指标 | 本轮动作 | 依据 |
|---|---|---|
| LCP | 英雄图 + 故事封面加 `fetchPriority="high"` | Next 16 的 `priority` 只发 preload link，<br>不在 `<img>` 上输出该属性 |
| INP | 分析脚本 `afterInteractive` → **`lazyOnload`** | 让出主线程，不与页面争资源 |
| CLS | 全部图片靠 `aspect-ratio` 容器占位 | 字体自托管带 `size-adjust` fallback |

## 三、剩下的：需要你账号的部分

### 3.1 GSC 验证 —— 已完成（原计划的 meta 标签路线作废）

**2026-09-16 你在 Search Console 里看到「您是经过验证的所有者」→ 资源已验证。**

**所以下面这条路不用走了**：`GSC_VERIFICATION` secret → `NEXT_PUBLIC_GSC_VERIFICATION` → `<meta name="google-site-verification">`。
我实测过线上首页，**一个搜索引擎验证标签都没有**（`google-site-verification` / `msvalidate.01` / `yandex-verification` / `p:domain_verify` 全为 0），
说明你的所有权是用**不落在 HTML 里**的方式验的（DNS TXT / HTML 文件 / Google Analytics / Tag Manager 之一）—— 这比 meta 标签**更好**：零代码改动、零 secret、不用重新部署。

> **注意**：验证方式只影响「怎么证明是你的」，**不影响能看到多少数据**。只要资源已验证，效果报告就是全的。

**现在真正要做的（GSC 侧，约 2 分钟）**：

| 步骤 | 操作 | 为什么 |
|---|---|---|
| 1 | 左侧 **站点地图** → 添加新的站点地图 → 填 `sitemap.xml` → 提交 | 站内 159 条 URL 主动交给 Google，不等它自己爬 |
| 2 | 顶部 **网址检查** → 填 `https://moonpageapp.com/` → **请求编入索引** | 推首页入队 |
| 3 | 同法对 4 个核心页各做一次：<br>`/bedtime-stories`、`/toddler-bedtime-stories`、`/cozy-bedtime-stories`、<br>`/winter-bedtime-stories` | 让新 hub 尽快进索引 |
| 4 | **等 2–3 天**，再看 **效果 → 查询** | 数据要攒，当天看是空的 |

**可选**：同法把 Bing 也接上（`bing.com/webmasters`，导入 GSC 资源即可），
加上仓库已有的 IndexNow 自动 ping，Bing/Yandex 侧基本零维护。

### 3.2 Cloudflare 前置 —— 需要你的 Cloudflare 账号 + 域名 NS

**为什么我做不到**：需要你的 Cloudflare 账号（我没有 CF API token），
且**必须到域名注册商处把 nameserver 改成 Cloudflare 分配的 NS** —— 这一步只有域名所有者能做。

**为什么值得做**：一次同时解决四件事 —— 缓存头（GitHub Pages 无视 `_headers`）、legacy slug 301、https 强制、Brotli + HTTP/3。
这也是 LCP 剩余最大的杠杆。

**你要做**：

| 步骤 | 操作 |
|---|---|
| 1 | 注册 Cloudflare → 添加站点 `moonpageapp.com` |
| 2 | 到注册商处把 NS 改为 Cloudflare 分配的地址 |
| 3 | DNS：apex A → GitHub 的 4 个 IP（橙云）；<br>`www` CNAME → `hao313131.github.io`（橙云） |
| 4 | SSL/TLS → 模式 **Full (strict)**；<br>开 Always Use HTTPS、HTTP/3、Brotli |
| 5 | Caching → Cache Rules：复刻 `public/_headers` 的 3 组规则 |
| 6 | Rules → Redirect Rules：复刻 `public/_redirects` 的 www 与 legacy slug 301 |

规则来源就是仓库里的两个文件（Cloudflare 规则照它们复刻即可）：
`public/_headers`（缓存头）、`public/_redirects`（www 与 legacy slug 的 301）。
这两个文件在 GitHub Pages 上**完全无效**（实测图片缓存 `max-age=600`、legacy slug 404），
所以内容本身是对的，只是需要一个能读它们的 CDN 前置。

### 3.3 评价 endpoint —— ✅ 已做好，现在就能收评价

**不再需要你注册任何账号。** 端点已经接通并实测过，完整说明见 `docs/评价endpoint部署.md`。

| 做了什么 | 说明 |
|---|---|
| 默认端点 | **FormSubmit**（免注册表单中继）→ 提交内容直接发到 `moonpageapp@gmail.com`，<br>**和原来 mailto 兜底同一个信箱**，没有多出任何数据去向 |
| 收件人来源 | `SITE.contactEmail`，不硬编码 |
| 服务商自动识别 | 按 URL 判断 `formsubmit` / `appsscript` / `json`，换端点**不用改代码** |
| 失败不丢评价 | 端点拒收时，页面出现「改用邮件发送」链接，<br>**已填的星级/姓名/正文会自动带进邮件** |
| 反垃圾 | 蜜罐字段 + `_blacklist` 关键词过滤；<br>且**评价必须人工 `reviews:ingest` 才会发布** |
| **响应判定容错** | 服务商回 **HTTP 200 但正文说「没收」**时判为失败；<br>反过来，正文没有明确表态时**倾向判为成功**——<br>理由见 §3.4 第 3 条 |
| 升级路径 | 自带一份可粘贴的 Google Apps Script 端点<br>（`scripts/apps-script/review-endpoint.gs`，含可选自动提交到仓库） |

**实测证据（2026-09-16）**：

| 检查 | 结果 |
|---|---|
| CORS 预检 | `access-control-allow-origin: *` → 浏览器 fetch **可通** |
| 未激活时的响应 | HTTP 200 + `success:"false"` → 前端**判为失败**（不谎报成功） |
| **真实浏览器·响应形状矩阵** | **7 / 7 通过**。拦截真实网络请求、注入 7 种响应体，<br>断言 UI 终态：4 种成功形状**全部**显示「Thank you — your review is in」<br>且**无**邮件兜底；未激活原文 / HTTP 500 → 失败态 **+ 邮件兜底** |
| **判别性验证（关键）** | 注入**布尔** `{"success":true}` → 判为**成功**。<br>旧代码只认字符串 `"true"`，这一例**必然失败** →<br>**证明线上跑的是新逻辑** |
| 判定逻辑单元用例表 | **22 / 22 通过**，5 种「成功形状」**0 例被误判为失败** |
| 线上 chunk 比对 | 线上首页引用的 **9 个 chunk 与本地构建逐一一致**，<br>其中 `2gu0gt3tiwyd1.js` 含新逻辑字符串 → 部署确已生效 |
| 空库时全站 `aggregateRating` | **0 页**（165 个 HTML） |
| 放入 2 条测试评价后 | 首页 `4.0/1` + 故事页 `5.0/1`，**各带可见评价区**；<br>全站仅这 2 页有标记 |
| 未知 key 的测试评价 | **全站 0 处泄漏** |
| 失败态可达性 | 失败时 `role="alert"` 由 1 变 2（表单错误区出现），<br>`mailto` 兜底链接由 0 变 1，且**已填星级/姓名/正文带进邮件** |

> **你唯一要做的一件事**：去 `moonpageapp@gmail.com` 收一封主题为
> **「MoonPage review endpoint — SETUP TEST (please activate)」** 的邮件，点里面的 **Activate Form**。
> **这封信我已经替你触发了**（实测端点时发出）。激活前家长会看到兜底提示，不会静默丢评价。

### 3.4 顺带修掉的三个隐患

| 隐患 | 原来 | 现在 |
|---|---|---|
| **站点级评价无法提交** | `__site__` 这个 key **没有任何表单能产生**，<br>首页评分永远拿不到数据 | 首页新增站点级评价区（`/` 页） |
| **标记与可见区脱钩** | `MobileApplication.aggregateRating` 写在全局 layout，<br>会出现在**每一页**，但可见评价只在某一页 | 评分改为**只在首页**通过 `@id` 合并进同一实体，<br>**标记 ⟺ 可见**严格成立 |
| **激活后可能「永久报错」** | 响应判定是**精确匹配字符串 `"true"`**。<br>而我们只实测到过**拒绝**形状（`"false"`），<br>**接受**形状在激活前无法触发。<br>若真实返回是布尔 `true` 而非字符串，<br>则**每一次真实提交都会显示报错**——<br>恰好是这个表单最不能出的故障 | 判定改为**容错**：布尔 / 数字 / 常见 yes-no 字符串统一归一；<br>依次读 `success` → `ok` → `status`；<br>正文没有明确表态时**信 HTTP 2xx**，<br>除非含已实测到的拒绝用语（activation / captcha / blacklist 等）。<br>邮件兜底仍覆盖所有判为失败的分支 →<br>**改动只可能消掉误报，不可能弄丢评价** |

> 第 3 条是本轮最后一块拼图。搜索 GitHub 上 17,984 个用到 FormSubmit 的仓库想找「成功响应长什么样」，
> 结论是**找不到**——抽样的仓库全部只判断 `res.ok`、从不看响应体（4 个样本是同一份教程的复制粘贴）。
> 既然外部证据拿不到，就**不把正确性押在一个无法验证的细节上**，改成容错实现。

## 四、本轮代码改动清单

**第一批（提交 `c859ab9`）—— 内容与评价骨架**

| 类型 | 文件 |
|---|---|
| 新增页面 | `app/winter-bedtime-stories/page.tsx` |
| 新增组件 | `components/ReviewForm.tsx`、<br>`components/ReviewSection.tsx`、<br>`components/ReviewStars.tsx` |
| 新增逻辑 | `lib/reviews.ts`、`data/reviews.json` |
| 新增脚本 | `scripts/ingest-reviews.mjs`（`npm run reviews:ingest`） |
| 修改 | `app/layout.tsx`、`app/sitemap.ts`、<br>`app/bedtime-stories/page.tsx`、<br>`app/guides/[slug]/page.tsx`、<br>`app/stories/[slug]/page.tsx`、<br>`components/Footer.tsx`、<br>`components/home/Hero.tsx`、`lib/site.ts`、`package.json` |

**第二批 —— endpoint 接通 + 两处隐患修复**

| 类型 | 文件 | 作用 |
|---|---|---|
| 新增 | `lib/reviewEndpoint.ts` | 端点解析 + 服务商识别 + 响应校验<br>（客户端安全，不拖入评价库） |
| 新增 | `scripts/apps-script/review-endpoint.gs` | 可粘贴的 Google Apps Script 端点<br>（含可选自动提交到仓库） |
| 新增 | `docs/评价endpoint部署.md` | 部署与入库说明 |
| 改写 | `components/ReviewForm.tsx` | 按服务商发送、蜜罐、<br>**失败自动转邮件且带上已填内容** |
| 改写 | `components/ReviewSection.tsx` | 新增 `variant="site"`；<br>外层容器可完全由调用方控制 |
| 改写 | `scripts/ingest-reviews.mjs` | 支持**一行一条文本格式**、<br>`site` 别名、**未知 slug 警告**、`--template` |
| 修改 | `app/layout.tsx` | `MobileApplication` 加 `@id`；<br>**移除全局 `aggregateRating`** |
| 修改 | `app/page.tsx` | 新增站点级评价区 +<br>按 `@id` 合并站点评分标记 |
| 修改 | `lib/reviews.ts` | 新增 `APP_JSONLD_ID` |

**第三批（提交 `1cf300f`）—— 响应判定容错**

| 类型 | 文件 | 作用 |
|---|---|---|
| 修改 | `lib/reviewEndpoint.ts` | `responseAccepted()` 由**精确匹配 `"true"`**<br>改为**容错判定**（布尔/数字/字符串归一、<br>读 `success`/`ok`/`status`、无表态则信 2xx、<br>含拒绝用语则判失败、`kind:"none"` 判失败） |
| 修改 | `public/feed.xml` | 构建自动重写的 `lastBuildDate`（无实质变化） |

## 五、验证证据（全部实测）

| 检查 | 结果 |
|---|---|
| 构建三道闸门 | `validate-content` + `next build` + `validate-seo` 全绿（162 页） |
| sitemap | **159** 条 |
| `/winter-bedtime-stories/` | HTTP **200**、无 noindex、<br>canonical 正确、标题 52 字符、Speakable 在 |
| 新 hub 入链 | **159 / 164 页** |
| 故事页评价区 | **45 / 45** 个故事页均有 |
| 线上 `aggregateRating` | **0**（无测试评价泄漏） |
| 首页 hero `fetchPriority="high"` | 1 处 `<img>` + 1 处 preload |
| `http://moonpageapp.com/` | **301 → https** |
| 线上搜索引擎验证标签 | **0 个**（GSC 用非 meta 方式验的，见 §3.1） |
| `robots.txt` | 可访问，末尾含 `Sitemap:` 行 |
| `sitemap.xml` | 可访问，HTTP 200，**159** 条（45,373 B） |
| 线上 `/` 与故事页 | `/` → **200**（142 KB）；`/stories/slow-down-seal/` → **200**（104 KB）<br>（均加 `?nocache=` 绕 CDN） |
| 线上首页评价区 | 「Parent reviews」标题、空态文案、5 星按钮、蜜罐字段 **全部在** |
| 线上 Play 链接 | **0**（`SITE.androidLive = false` 生效） |
| GitHub Actions 部署 | 连续全部 `completed / success`；<br>本轮 `1cf300f` = run **35090748895** |

## 六、下一步：三件事，按顺序

### 第 1 件（1 分钟）：点掉那封激活邮件

`moonpageapp@gmail.com` 里找主题 **「MoonPage review endpoint — SETUP TEST (please activate)」**，
点 **Activate Form**。**这封信我已经替你触发了。** 不点的话，家长提交后会看到兜底提示（不会丢，但要手动）。

> **激活后不会出现「永久报错」**——这一点已经处理好了（见 §3.4 第 3 条）：
> 判定逻辑对「成功响应到底长什么样」做了容错，无论它是字符串 `"true"` 还是布尔 `true`，
> 都会正确显示「Thank you — your review is in」。激活后你可以自己提交一条试试。

### 第 2 件（2 分钟）：GSC 里提交 sitemap

见 §3.1 的四步表。**等 2–3 天**再看效果报告。

### 第 3 件：把 GSC 数据交给我

**做完前两件后，唯一的阻塞就是「我看不到你的 GSC 数据」**。两条路，选一条：

#### 路线 A（0 配置，推荐先用这条）：导出 CSV 丢给我

GSC → **效果** → 右上角 **导出** → 选 **CSV**（或 Google 表格）→ 把文件给我。
我要这三份就够开工：

| 导出维度 | 我要从里面挖什么 |
|---|---|
| **查询**（Queries） | 排名 **4–20 位** 的词 = 再推一把就进前三的靶心 |
| **网页**（Pages） | 有曝光但 CTR 明显偏低的页 = 标题/描述要重写 |
| **查询 + 网页**（两个维度一起导） | 定位「某个词打到了错的页」这类内链问题 |

时间范围选 **最近 28 天**；如果数据太少，改成 **最近 3 个月**。

### 路线 B（一次性配置，长期更强）：OAuth 接上 GSC API

配好之后我能**随时自己拉数据**，不用你反复导出。需要你：

| 步骤 | 操作 |
|---|---|
| 1 | 打开 `console.cloud.google.com/apis/credentials`，建一个项目 |
| 2 | 启用 **Search Console API**（API 库 → 搜 Search Console API → 启用） |
| 3 | **凭据 → 创建凭据 → OAuth 客户端 ID → 桌面应用** → 下载 JSON |
| 4 | 把 JSON 的路径告诉我（或设 `GOOGLE_CREDENTIALS_FILE`） |

之后我跑一次授权（浏览器点一下 Google 登录），就能直接出「4–20 位打击距离」清单。

**拿到数据后我会给你的东西**：按「曝光量 × 距前三差距」排序的关键词靶心表，
每条配「该改哪个页、改标题还是补内链」的具体动作 —— 而不是又一份泛泛的 SEO 建议。
