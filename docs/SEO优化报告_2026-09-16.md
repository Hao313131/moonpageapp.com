# MoonPage 全站 SEO 优化报告

**日期：** 2026-09-16
**对象：** https://moonpageapp.com （Next.js 16 静态导出 → GitHub Pages，154 个页面）
**目标：** 提高搜索结果位置 + 提高点击率与下载转化
**范围：** 全站技术审计（153 个页面逐页解析）、元数据重写、内链重建、结构化数据补强、构建期防回归闸门

---

## 一、一句话结论

MoonPage 的 SEO 底子**已经很硬**——全站 canonical、JSON-LD、sitemap 优先级梯度、image sitemap、RSS、IndexNow、AI 爬虫白名单、按主题/年龄拆分的 9 个 hub + 22 个合集 + 64 篇指南，这套结构不是新手站。

所以这次没有从零堆关键词，而是去找**真正在漏钱的地方**。审计下来是三件可量化的事：

| 优先级 | 问题 | 规模 | 性质 |
|---|---|---|---|
| P0 | 一个指向 404 的 Google Play 下载按钮 | **147 / 154 页** | 转化漏洞 |
| P0 | 标题被 Google 截断（丢了钩子） | **38 / 151 页** | 点击率 |
| P0 | 描述被 Google 截断（丢了钩子或 CTA） | **57 / 151 页** | 点击率 |
| P1 | 合集 / 故事 / 指南三层内容互不连接 | 全站 | 主题权威传导 |
| P1 | 站长验证码是占位符 → GSC 完全失明 | 全站 | 无法度量 |

全部已修复，并加了**两道构建期闸门**，以后同类问题会直接让构建失败、上不了线。

---

## 二、改动前后（硬数字）

审计方式：逐页解析 `out/` 里真实的 HTML（不是看源码），长度按**浏览器实际渲染的字符数**算（`&amp;` 记 1 个字符，不是 5 个）。

| 指标 | 改前 | 改后 | 意义 |
|---|---|---|---|
| 挂失效 Play 按钮的页面 | **147** | **0** | 每页少一个通向 404 的主 CTA |
| 标题 > 60 字符（会被截断） | **38** | **0** | 钩子不再被吃掉 |
| 最长标题 | 74 字符 | 60 字符 | 全部落在截断线内 |
| 描述 > 158 字符（会被截断） | **57** | **0** | — |
| 最长描述 | 305 字符 | 155 字符 | 45 个故事页从"砍一半"变成"完整可读" |
| 描述平均长度 | 157.4 | 129.8 | 从"刚好超线"退回安全区 |
| 标题平均长度 | 51.0 | 40.5 | 去掉品牌后缀换来的有效空间 |
| 内链仅 2 条入链的指南 | 10 篇 | 6 篇 | 长尾页不再孤岛 |
| JSON-LD 块 | 约 700 | **864**（0 个解析失败） | 新增 ItemList / isAccessibleForFree 等 |
| 构建期 SEO 闸门 | 1 道（只看数据） | **2 道**（数据层 + 产出层） | 同类问题无法再上线 |

---

## 三、逐项修了什么

### 3.1 【P0 · 转化】147 个页面上的 Google Play 按钮指向 404

**实测证据：**

- `https://play.google.com/store/apps/details?id=com.echorealmmedia.moonpage` → **HTTP 404**
- 在 Google Play 搜 "MoonPage" → 无结果
- 试过 `com.moonpage` / `com.moonpageapp` / `com.echorealm.moonpage` → 全部 404
- 但 App Store 侧是真实的：`itunes.apple.com/lookup?id=6788652725` → 上架于 2026-07-27，当前版本 2026-08-25，Books 分类，免费，4+

**问题：** 首页的 Hero、Pricing、底部吸底条都传了 `hidePlay`（所以首页是干净的），但**其余 147 个页面没有**——9 个 hub、45 个故事页、64 个指南、22 个合集、FAQ、404 页，全都渲染了一个 "Download on Google Play" 按钮，点进去是 404。

**为什么这条最贵：** 一个失效的主 CTA 同时损失两样东西——**那一次下载**，和**那位家长对品牌的信任**。她不会认为是商店的问题，只会觉得这个 app 不靠谱。

**修法：** 把"哪个平台有真实 listing"从 20 个文件的 prop 收拢成 `lib/site.ts` 里的一个布尔量 `androidLive: false`。`StoreButtons` 读它决定是否渲染 Play 徽章。Android 上架那天改这一个布尔量，全站徽章、JSON-LD、FAQ 答案同时跟上。

**连带修正：** FAQ 里原来写 "Yes — MoonPage runs on iOS and Android phones and tablets."（`lib/hubFaqs.ts`）、"MoonPage is available on both iOS through the App Store and Android through Google Play."（`lib/faq.ts`）、"iPhone, iPad, and Android? Yes —"（`app/bedtime-stories-app`）。这三条在 Play listing 不存在的前提下是**不实陈述**，已改为「App Store 现已上架，Android 开发中」。JSON-LD 里的 `operatingSystem` 也从 `"iOS, Android"` 改成跟随同一个布尔量。

### 3.2 【P0 · 点击率】标题模板把 38 个标题推过 60 字符

`app/layout.tsx` 的 `title.template` 是 `%s · MoonPage`，给**每一个**标题硬加 11 个字符。结果 38 个标题超出 Google 约 60 字符（≈580px）的截断线，而且**被切掉的永远是尾巴**——也就是钩子所在的位置：

| 改前（截断处） | 长度 |
|---|---|
| `The 20-Minute Bedtime Routine That Actually Sticks (Ages 2–7) · MoonPage` | 72 |
| `Bedtime Stories for 4-Year-Olds: Longer Books, Bigger Questions · MoonPage` | 74 |
| `Sleep Regressions by Age: What's Normal & How Long They Last · MoonPage` | 71 |
| `Bedtime Stories for Kids Ages 2+ — Calm Nights Start Here · MoonPage` | 68 |

`(Ages 2–7)`、`Bigger Questions`、`Calm Nights Start Here` 这些**正是让人点进去的部分**，全在截断线外。

**修法：** 模板改为 `%s`（不再附加品牌），页面标题自身承担全部信息。品牌不会消失——Google 会在标题**上方**单独印站点名，来源就是站内的 WebSite / Organization JSON-LD。等于用最贵的像素去重复一个搜索者早就知道、而且 Google 免费帮你印的词，是纯浪费。

顺带把 5 个仍然超线的指南标题压回 60 以内（保留钩子，砍冗余词），并把 `/support/` 从 18 字符的 "Support" 改成 45 字符的 "MoonPage Support: Cancel, Restore & Contact Us"——support 页是订阅类产品**意向最高**的页面之一，"cancel" 和 "restore purchases" 正是用户搜到它之前打的那几个字。

### 3.3 【P0 · 点击率】45 个故事页 + 9 篇指南的描述被截断

故事页原来的描述是 `hook + 90 字符的 CTA` 拼接，落点 **190–305 字符**。Google 在 ~155 字符处切，于是：

- 好一点的情况：CTA 被砍掉（白写）
- 坏一点的情况：`hook` 本身被切在句子中间，读起来像坏了

**修法：** 新增 `storyMetaDescription()`（`lib/site.ts`）——按**句子边界**决定放几句 `hook`，放不下就整句丢弃，再补一句短 CTA。丢弃优于截断：Google 截断是切在词中间，读起来像损坏；丢句读起来只是简短。

```ts
// 改前：The fastest seal in the bay. A whole ocean going past in a blur. What is Sully
//        swimming straight past at top speed? Open it free in MoonPage tonight — illustrated,
//        narrated, offline. No ads, no login.            ← 200 字符，截断
// 改后：The fastest seal in the bay. A whole ocean going past in a blur.
//        A cozy picture book for ages 2+ — read it free in MoonPage tonight.   ← 140 字符，完整
```

同时压缩了 9 篇超线指南描述、`/guides/`（185→147）、`/stories/`（256→148）、`/collections/sleepy-bedtime-stories/`（165→148）。

### 3.4 【P1 · 排名】三层内容互不连接

站点有三层讲同一件事的页面：

```
collections/<slug>   主题书架     "Animal Bedtime Stories"
stories/<slug>       一本书       "Slow Down, Seal!"
guides/<slug>        家长的问题   "Why Toddlers Wake at Night"
```

**改前它们是三个孤岛：**

- 故事页 → 只链其他故事 + **一个硬编码的**指南（`choosing-bedtime-books`），跟这本书的主题无关
- 合集页 → 只链自己的故事 + 4 个其他合集，**一条指南链接都没有**
- 指南页 → 只链其他指南，**一条合集链接都没有**

结果：一篇讲"怕黑"的指南，只能靠运气被怕黑主题的故事页指到；`bedtime-for-twins` 这类冷门指南全站只有 2 条入链——典型"准孤儿页"，Google 没什么理由把它当话题的一部分。

**修法：** 新建 `lib/internalLinks.ts`，用**一张以 story tag 为键的手写映射表**（25 个 tag，每个 3 篇真正相关的指南），派生出三个方向的链接：

| 方向 | 用在哪 |
|---|---|
| story → guides | 故事页新增「Keep going with this theme」 |
| collection → guides | 合集页新增「Bedtime advice for this theme」+ 一个 ItemList JSON-LD |
| guide → collections | 指南页新增「Stories for this」 |

用 tag 做键而不是 slug，是因为 tag 表小、稳定（跟 app 的 `story-tags.ts` 同步）、而且**本来就是决定一本书属于哪个合集的依据**。一张表三个用途，三层不会漂移。表里的 slug 在构建时解析，失效就静默丢弃——改个指南名只会少一条链接，不会炸构建。

### 3.5 【P1 · 实体识别】结构化数据

| 改动 | 位置 | 作用 |
|---|---|---|
| `Organization.sameAs` 加 App Store 链接 | `layout.tsx` | 把站点、app id、品牌绑成同一个知识图谱节点 |
| `WebSite.alternateName` = `["MoonPage App", "MoonPage: Cozy Bedtime Stories"]` | `layout.tsx` | 让 "moonpage app" 和 App Store 的完整上架名都指向同一实体 |
| `MobileApplication.installUrl` + `sameAs` | `layout.tsx` | 应用安装意图更明确 |
| `Book.isAccessibleForFree: true` + `typicalAgeRange: "2-7"` | `stories/[slug]` | 让"免费"能出现在书籍结果上，是产品事实 |
| 合集页新增相关指南 `ItemList` | `collections/[slug]` | 让书架对爬虫呈现为"话题簇"而非一堆封面 |
| 全站 `hreflang` = `en` + `x-default` | `pageMetadata()` | 声明单一语言，防止聚合器猜错 |

**没有做的一件事（故意）：** 没有加 `aggregateRating`（星级）。查过 App Store 的真实数据：`averageUserRating: 0`、`userRatingCount: 0`。**零评价的情况下加星级是编造，会触发 Google 的反欺骗手动处罚**——这是收益 20–40% 的点击率杠杆，也是唯一一个必须等真实评价才能拉的杠杆，见第六节。

**顺手清理：** JSON-LD 里原来写 "trusted by thousands of moms"。可见文案要不要保留是营销判断，但**结构化数据里的主张 Google 会当成可核验的事实**，而且它能直接对着自己的商店索引核。已从两处 JSON-LD 描述中移除，只保留产品事实。

### 3.6 【P1 · 收录】索引卫生

| 改动 | 原因 |
|---|---|
| `robots.txt` 新增 `Disallow: /404` | 静态构建把 `app/not-found.tsx` 导出了两次：`404.html`（GitHub Pages 服务缺失路径用）和 `404/index.html`——后者是**可抓取的真实 URL**，会作为首页 chrome 的近似重复进索引 |
| `/search/` 加 `noindex, follow` | 内容完全由客户端从其他页面渲染，索引它只会在一个没人搜的 URL 下造一份 `/stories` + `/guides` 的薄重复。保留 `follow`，爬虫照样被送进真实页面 |
| 清掉 4 个 `REPLACE_WITH_*` 占位符 | 见 4.1 |

### 3.7 【P1 · 防回归】新增双层构建闸门

这次的三个 P0 有一个共同点：**在源码里都看不出来**。标题模板的后果、描述拼接的后果、Play 按钮的后果，只有把**真实 HTML 渲染出来**才看得见。

所以加了两道门：

**第一道（已有，本次加长检查）— `scripts/validate-content.ts`**
数据层，`next build` 之前跑。新增标题/描述长度校验，上限从 `lib/site.ts` **import**（不是重新写一遍数字），所以"写文案时对照的预算"和"闸门执行的预算"不可能漂移。

**第二道（新增）— `scripts/validate-seo-output.mjs`**
产出层，`next build` 之后跑，逐页解析 `out/` 里的真实 HTML：

- `<title>` 存在且 ≤ 60
- meta description 存在且 ≤ 158
- 恰好一个 `<h1>`
- canonical 存在且**等于该页自己的 URL**
- `<head>` 里没有残留的 `REPLACE_WITH_`
- 有 JSON-LD
- **没有任何锚点指向不存在的商店 listing**
- sitemap 里每个 `<loc>` 都能对应到一个真实构建出来的页面
- 可索引页面之间没有重复 `<title>`

**踩到的坑（值得记下来）：** 第一版闸门报了 9 个"超长"，全是假阳性——它在数**原始字节**，而 `&amp;` 是 5 个字符、`&#x27;` 是 6 个字符，渲染到结果页上都是 1 个。按字节量会去压那些本来合规的标题，**结果是文案被砍、检查器还报绿**。已加实体解码后再测量。

---

## 四、必须你动手的 5 件事（我改不了）

### 4.1 GSC 站长验证码还是占位符 —— 你现在对搜索端完全失明

`app/layout.tsx` 里原来是四个字面占位符：

```html
<meta name="google-site-verification" content="REPLACE_WITH_GSC_CODE"/>
<meta name="msvalidate.01" content="REPLACE_WITH_BING_CODE"/>
<meta name="yandex-verification" content="REPLACE_WITH_YANDEX_CODE"/>
<meta name="p:domain_verify" content="REPLACE_WITH_PINTEREST_CODE"/>
```

已改成**读环境变量**：设了就输出，没设就**完全不输出这个标签**（错误的 token 比没有更糟，看起来像配好了）。占位符已从产出里清掉。

```bash
# 构建前设置（或写进 .env / CI secrets）
NEXT_PUBLIC_GSC_VERIFICATION=...        # Search Console → 设置 → 所有权验证
NEXT_PUBLIC_BING_VERIFICATION=...       # Bing Webmaster Tools（同时喂 Yahoo）
NEXT_PUBLIC_YANDEX_VERIFICATION=...     # 可选，配合 IndexNow
NEXT_PUBLIC_PINTEREST_VERIFICATION=...  # 可选，真实母婴/DIY 流量
```

或者走 DNS TXT 验证，那这几个变量永远不用设。

**为什么这是第一优先级：** 没验证 = 看不到展现量、平均排名、点击率。**后面所有 CTR 实验都无法度量。** 验证完立刻能做两件高价值的事：提交 `sitemap.xml`；导出「排名 4–20 位的查询」——那是投产比最高的一批词，只需要一点点推力就能进前 3。

### 4.2 GitHub Pages 的 "Enforce HTTPS" 没开

**实测：** `curl http://moonpageapp.com/` → **HTTP 200**（不是 301）。同一个 URL 的 https 版本也是 200。

这是同一份内容在两个 scheme 下都能访问——经典的重复内容 + 安全问题。

**怎么做：** GitHub 仓库 → Settings → Pages → 勾上 **Enforce HTTPS**。（`www → apex` 的 301 是**正常**的，实测 301 到 `https://moonpageapp.com/`，这块不用动。）

### 4.3 `_headers` 和 `_redirects` 在 GitHub Pages 上是死文件

`public/_headers` 和 `public/_redirects` 写得很认真，注释里也写明了 GitHub Pages 会忽略它们——但**现在的实际后果是**：

| 你以为生效了 | 实测结果 |
|---|---|
| legacy slug 301 到最近的 hub | `/stories/milos-little-boat/` → **HTTP 404**，链接权重直接丢 |
| 图片缓存 1 天 + stale-while-revalidate | `cache-control: max-age=600`（**只有 10 分钟**） |
| hashed 构建资源 immutable | 未生效 |
| www → apex 301 | ✅ 生效（GitHub Pages 自己的自定义域重定向） |

**怎么做（按性价比排序）：**

1. **把 GitHub Pages 前面套一层 Cloudflare（免费版）** —— 一次解决三件事：Edge 上强制 https、缓存规则（图片 10 分钟 → 1 天）、URL 重定向规则（legacy slug 的 301 终于生效）。这是**单点杠杆最高的一次基础设施改动**。
2. 或者迁到 Cloudflare Pages / Netlify —— 这两个原生读 `_headers` / `_redirects`，你写好的配置直接就能用。

在这之前，那些 legacy slug 会一直 404 并持续丢权重。

### 4.4 Android 上架状态确认

我按「Play listing 不存在」处理了（证据见 3.1）。如果实际情况是**已在审核中、即将上架**，那我的文案改法（"Android 开发中"）仍然正确，等上架那天把 `lib/site.ts` 里的 `androidLive` 改成 `true` 即可，全站自动跟上。

如果**其实已经有 listing、只是包名不同**，请把真实包名给我——那 `hidePlay` 这一整块要撤回。

另外 `app/privacy-choices/page.tsx` 和 `app/support/page.tsx` 里都有"Android 用户如何取消订阅"的操作步骤，而 README 明确写了这些法律/支持文案是从 `moonpage-legal` 仓库**逐字移植、不允许漂移**的。所以我**没有动**它们——但一个装不上 Android 版的用户读到 Android 取消指引会困惑。要不要同步改，得你先改源仓库。

### 4.5 "Trusted by thousands of moms" 这句

现在 `lib/site.ts` 的 `trustLine` 和可见文案里都有这句，而 App Store 侧是 **0 条评价**。

你的 `docs/SEO_MURDER_MYSTERY_PLAN.md` 自己把这条列为 P0：「在拿到真实评价前就铺'好评/星级'属于 Google 反欺骗政策红线，会触发手动处罚」。

我**只从 JSON-LD 里移除了**（结构化数据是可核验主张，风险最高），**可见营销文案原样保留**——因为这句话可能真的基于你手上的下载数据，那是你的营销判断，不该由我替你改。但请确认一下它站得住；如果站不住，`lib/site.ts` 里改一行就行。

---

## 五、内容缺口（关键词层面）

现有 9 个 hub 覆盖了：bedtime stories、toddler、preschool、read-aloud、by-age、cozy、lullaby、app、picture books。

对照家长真实搜索意图，下面这些**有明确搜索量但站内没有落地页**：

| 建议新增 hub | 搜索意图 | 为什么值得做 | 落地成本 |
|---|---|---|---|
| `/short-bedtime-stories/` | "short bedtime stories" / "5 minute bedtime stories" | 高意图长尾，"短"是家长最常加的前缀；现有产品完全能满足（故事本就短） | 低——复用现有故事网格 |
| `/free-bedtime-stories/` | "free bedtime stories" / "free bedtime stories for kids" | 免费意图量极大，且产品**真的有免费层**，是诚实匹配 | 低 |
| `/bedtime-stories-for-babies/` | "bedtime stories for babies" | **指南已经有了**（`bedtime-stories-for-babies`），但缺对应 hub——指南在往一个不存在的落地页导流 | 低 |
| `/christmas-bedtime-stories/`、`/halloween-bedtime-stories/` | 季节性峰值 | 节日前 6–8 周开始吃流量，且每年复现 | 中——需要主题故事 + 合集 |
| `/sleep-stories-for-kids/` | "sleep stories for kids" | 与 lullaby hub 相邻但词不同，是 Calm 类产品的核心词 | 低 |

**优先级建议：** 先做 `/short-bedtime-stories/` 和 `/free-bedtime-stories/`。这两个不需要新写故事内容，只需要新的 hub 文案 + 复用现有 StoryGrid + 挂上对应的 FAQ 和内部链接——是这批里投产比最高的。

**另一个已验证的事实：** 64 篇指南**全部**有 FAQPage 结构化数据（64/64），但只有 13 篇有 `<ol>` 步骤列表（因此有 HowTo）。HowTo 富结果 Google 在 2023 年后已大幅降级，**不建议为了 schema 去硬塞步骤列表**——那是为结构化数据写内容，本末倒置。

---

## 六、还没拿到的大杠杆：星级富结果

**这是全站剩下最大的一块点击率收益（SERP 星级通常提升 CTR 20–40%），现在拿不到，唯一原因是评价数为 0。**

实测 App Store：

```
averageUserRating: 0
userRatingCount:    0
```

**不能做的事：** 自己编一个 `aggregateRating`。零评价的星级是编造，Google 有反欺骗政策，且它**能对着 Apple/Google 的商店索引核验**。

**能做、且顺序很重要：**

1. **先要评价**（这是产品侧动作，不是 SEO 动作）
   - App Store：`SKStoreReviewController` / `requestReview`，**在用户读完一个故事、心情好的那一刻**弹，不要在启动时弹
   - 目标不是"很多评价"，而是**凑够能上 schema 的最低量**（有真实数据就能上）
2. **拿到之后**，把 `AggregateRating` 挂到两处：
   - `MobileApplication`（首页）
   - `Book`（故事页——书类目有星级结果时非常显眼）
3. 数据源用 App Store 的公开评分，且**必须写真实值**。

代码位置已经预留好了：`app/layout.tsx` 里 `organizationJsonLd` 上方那段注释明确写了"pre-launch, no real reviews yet"。等真实评价就位，加进去即可。

---

## 七、怎么验证有效（度量口径）

改动已全部落在本地构建里，**但还没推上线**（我没执行 git push）。

**上线后按这个节奏看：**

| 时间 | 看什么 | 在哪看 |
|---|---|---|
| 第 1–3 天 | 新标题/描述被重新抓取（GSC → 网址检查 → 请求编入索引，抽 10 个代表性页面） | GSC |
| 第 1 周 | IndexNow 推送 + sitemap 重新提交；`/404` 从索引里消失 | GSC 覆盖率 |
| 第 2–4 周 | **分类词的 CTR 变化**（`bedtime stories for kids`、`toddler bedtime stories`、`lullaby stories`）——这是本次改动的主指标 | GSC 效果 → 查询 |
| 第 2–4 周 | **平均排名**是否开始前移（内链重建通常 2–4 周见效） | GSC 效果 → 查询 |
| 第 4 周 | 导出「排名 4–20」的查询清单，逐条做定向内容补强 | GSC |
| 持续 | 商店点击转化（Play 按钮移除后，App Store 点击率应上升） | Umami `download-click` 事件，按 `data-umami-event-placement` 拆 |

**判断改动是否真的生效，看这两个信号：**

1. **CTR 上升而排名不变** → 标题/描述改动生效（这正是本次 P0 的目标）
2. **排名上升** → 内链重建 + 结构化数据生效（慢变量，2–4 周起）

如果两者都没动，先查 GSC 覆盖率报告里有没有新的抓取问题——那说明是抓取层而不是内容层。

---

## 八、改动文件清单

```
M  app/layout.tsx                    标题模板、验证码 env 化、JSON-LD 三处
M  app/stories/[slug]/page.tsx       描述生成器、主题内链块、Book schema
M  app/stories/page.tsx              标题/描述重写
M  app/guides/[slug]/page.tsx        合集内链块
M  app/guides/page.tsx               标题/描述重写
M  app/collections/[slug]/page.tsx   指南内链块 + ItemList JSON-LD
M  app/support/page.tsx              标题从 18 → 45 字符
M  app/faq/page.tsx                  标题含核心问题词
M  app/search/page.tsx               noindex, follow
M  app/robots.ts                     Disallow /404
M  app/bedtime-stories-app/page.tsx  FAQ 的 Android 陈述修正
M  components/StoreButtons.tsx       Play 徽章收拢到 SITE.androidLive
M  lib/site.ts                       androidLive、SERP 预算、fitDescription、storyMetaDescription、hreflang
M  lib/guides.ts                     5 个标题 + 11 条描述压回预算内
M  lib/collections.ts                1 条描述压回预算内
M  lib/hubFaqs.ts                    FAQ 的 Android 陈述修正
M  lib/faq.ts                        FAQ 的 Android 陈述修正
M  scripts/validate-content.ts       数据层长度闸门
M  package.json                      build 链末尾接入产出层闸门
A  lib/internalLinks.ts              tag → guides 映射 + 三个派生方向
A  scripts/validate-seo-output.mjs   产出层 SEO 闸门（逐页解析真实 HTML）
```

**构建状态：** `npm run build` 全绿。

```
[validate-content] OK — 45 stories, 64 guides, 22 collections passed the SEO gate.
✓ Compiled successfully in 2.1s
[validate-seo-output] OK — 152 pages: titles ≤ 60, descriptions ≤ 158,
                      canonicals exact, one <h1> each, sitemap resolves.
```

---

## 附：SEO 技能脚本的运行情况（诚实说明）

| 脚本 | 状态 | 原因 |
|---|---|---|
| `validate:content` / `validate:seo`（本次新增） | ✅ 跑通 | 不依赖外部凭据 |
| `trend_scout.py` | ⚠️ 跑通但**结果不可用** | Reddit 五个子版全部 **HTTP 403 Blocked**（无凭据）；Hacker News 是技术圈，与育儿无关；Google Trends 返回通用趋势。最终只产出 1 条 20/100 分的角度（"disallow AI training"），与本站无关 |
| `content_attack_brief.py` | ❌ 无法运行 | 需要 `AHREFS_TOKEN` + GSC OAuth |
| `gsc_client.py --striking` | ❌ 无法运行 | 需要 GSC OAuth，而 GSC 属性**还没验证**（见 4.1） |

**结论：** 这个技能里真正高价值的两个脚本（`content_attack_brief` 的竞品缺口 + `gsc_client --striking` 的 4–20 位查询）都卡在**同一件事**上——GSC 没验证、没有 Ahrefs token。

`trend_scout` 对**育儿/童书**这个垂直基本无效，因为它的信号源是 HN/Reddit/X，而不是母婴社区。真要抓这个垂直的趋势，更靠谱的是：Google Trends 直接查 `bedtime stories` 的 related queries、App Store 的 "You Might Also Like" 竞品关键词、以及 GSC 里已经有点击的长尾词。

**4.1 做完之后，`gsc_client --striking` 立刻就能用，那才是这次优化之后该跑的下一步。**
