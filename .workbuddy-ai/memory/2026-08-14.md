# 2026-08-14

## 主页隐藏 Google Play 徽章（iOS 首发过渡）
- 在共享组件 `components/StoreButtons.tsx` 的 `StoreButtons` 上加 `hidePlay?: boolean` 开关：为 true 时不渲染 `PlayStoreLink`，并让单独的 App Store 徽章 `grid-cols-1 justify-items-center` 居中、限宽 `max-w-[15rem] sm:max-w-xs`，保持美观。
- 同时在 `AppStoreLink` / `PlayStoreLink` 增加可选 `className` 透传。
- 主页启用 `hidePlay`：Hero、Pricing 的 `StoreButtons`，以及全局吸底 `StickyCta`（在 layout 里，主页也会显示，所以一并隐藏，否则主页仍看得到 Google Play）。
- 文案里去掉 "or Google Play"：`Trust.tsx` 的 FACTS[2]、`Pricing.tsx` 第14行，改为只提 App Store。
- 注：Footer / 其它页面未改动；StickyCta 是全局组件，隐藏 Google Play 会影响所有页面的吸底条（符合“安卓未上线”现状）。如需仅限主页，可改为按路由判断。
- 校验：`tsc --noEmit` 与 `eslint` 均通过。

## 苹果按钮对齐修复（同日下午）
- `components/StoreButtons.tsx` 的 `StoreButtons` 增加 `badgeAlign?: "responsive" | "center"`（默认 responsive）。`hidePlay` 单按钮布局：`badgeAlign="responsive"` = `grid-cols-1 justify-items-center md:justify-items-start`（移动端居中、桌面端左对齐，匹配 Hero 左对齐文案）；`badgeAlign="center"` = 始终居中。
- 应用：Hero 用默认 responsive（桌面左对齐）；Pricing（`components/home/Pricing.tsx`）与吸底条（`components/StickyCta.tsx`）显式 `badgeAlign="center"`（它们本就是居中版块，保持居中）。
- 校验：`tsc --noEmit` 与 `eslint` 均通过。

## 后续三处微调（同日下午）
- TikTok 链接隐藏、IG 保留：`components/SocialLinks.tsx` 的 `SocialLinks` 标准关注组合里把 `<TikTokLink>` 用 JSX 注释掉（IG 单独居中保持美观）；`app/layout.tsx` 的 organization JSON-LD `sameAs` 去掉 `SITE.tiktokUrl`，只留 IG。`lib/site.ts` 的 tiktokUrl/tiktokHandle 配置保留未删（便于恢复）。`TikTokLink`/`TikTokGlyph` 组件定义保留（仅不再渲染）。
- 文案：`app/get/page.tsx` 的 metadata 标题与页面 h1 两处 “Make bedtime feel calm again.” 改为 “One more story, and off to sleep.”（与主页 Hero 一致）。
- 页脚版权：`components/Footer.tsx` 删除底部 `© {year} {operator}. Made with care, one story at a time.` 整段（MinimalFooter 本就没有，不受影响）。
- 校验：`tsc --noEmit` 与 `eslint` 均通过。
