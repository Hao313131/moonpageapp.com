# 2026-08-14

## 主页隐藏 Google Play 徽章（iOS 首发过渡）
- 在共享组件 `components/StoreButtons.tsx` 的 `StoreButtons` 上加 `hidePlay?: boolean` 开关：为 true 时不渲染 `PlayStoreLink`，并让单独的 App Store 徽章 `grid-cols-1 justify-items-center` 居中、限宽 `max-w-[15rem] sm:max-w-xs`，保持美观。
- 同时在 `AppStoreLink` / `PlayStoreLink` 增加可选 `className` 透传。
- 主页启用 `hidePlay`：Hero、Pricing 的 `StoreButtons`，以及全局吸底 `StickyCta`（在 layout 里，主页也会显示，所以一并隐藏，否则主页仍看得到 Google Play）。
- 文案里去掉 "or Google Play"：`Trust.tsx` 的 FACTS[2]、`Pricing.tsx` 第14行，改为只提 App Store。
- 注：Footer / 其它页面未改动；StickyCta 是全局组件，隐藏 Google Play 会影响所有页面的吸底条（符合“安卓未上线”现状）。如需仅限主页，可改为按路由判断。
- 校验：`tsc --noEmit` 与 `eslint` 均通过。

## 苹果按钮对齐修复（同日下午）
- `components/StoreButtons.tsx` 的 `StoreButtons` 增加 `badgeAlign?: "responsive" | "center"`（默认 responsive）。`hidePlay` 单按钮布局：`badgeAlign="responsive"` = `grid-cols-1 justify-items-center md:justify-items-start`（移动端居中、桌面端左对齐，匹配 Hero 左对齐文案）；`badgeAlign="center"` = 始终居中。
- 应用：Hero 用默认 responsive（桌面左对齐）；Pricing（`components/home/Pricing.tsx`）与吸底条（`components/StickyCta.tsx`）显式 `badgeAlign="center"`（它们本就是居中版块，保持居中）。
- 校验：`tsc --noEmit` 与 `eslint` 均通过。

## 后续三处微调（同日下午）
- TikTok 链接隐藏、IG 保留：`components/SocialLinks.tsx` 的 `SocialLinks` 标准关注组合里把 `<TikTokLink>` 用 JSX 注释掉（IG 单独居中保持美观）；`app/layout.tsx` 的 organization JSON-LD `sameAs` 去掉 `SITE.tiktokUrl`，只留 IG。`lib/site.ts` 的 tiktokUrl/tiktokHandle 配置保留未删（便于恢复）。`TikTokLink`/`TikTokGlyph` 组件定义保留（仅不再渲染）。
- 文案：`app/get/page.tsx` 的 metadata 标题与页面 h1 两处 “Make bedtime feel calm again.” 改为 “One more story, and off to sleep.”（与主页 Hero 一致）。
- 页脚版权：`components/Footer.tsx` 删除底部 `© {year} {operator}. Made with care, one story at a time.` 整段（MinimalFooter 本就没有，不受影响）。
- 校验：`tsc --noEmit` 与 `eslint` 均通过。

## 故事库「最新排序」（同日下午）
- 需求：「library 全部用最新排序和封面图」。
- 范围判定：「library」= `/stories` 故事库页面（其余是 featured 精选 / 按主题分组，非时间序）。封面图：`StoryGrid`、`StoryShowcase`、集合页早已全部用 `storyCoverSrc(s.file)`（4:3），该项已满足，无需改动。
- “最新”依据：数据层 `lib/stories.ts` 的 `STORIES` 按 `b01`→`b45` 编号顺序声明，无每篇发布日期字段。按最合理约定：编号越大＝越晚加入（b45 最新），故倒序，b45 排最前。
- 实施（最小副作用）：`lib/stories.ts` 新增派生 `export const LATEST_STORIES = [...STORIES].reverse()`；`app/stories/page.tsx` 的封面网格与 ItemList JSON-LD 均改用 `LATEST_STORIES`，并将其从 `STORIES` 导入改为 `LATEST_STORIES`。`FEATURED_STORIES`、集合页、首页精选不动，避免误伤别的展示区。
- 校验：`tsc --noEmit` 与 `eslint` 均通过；`/stories` 页面已无 `STORIES` 残留引用。
- 备注：若用户后续要“全站（含首页精选、集合页）都按最新”，再决定是反转 `STORIES` 本身还是各展示区各自倒序。

## 故事库对齐 App 书架顺序与封面（同日下午，修正上一轮）
- 用户澄清：「最新排序」其实指“和 App 一样的排序和封面图”，不是按编号倒序。上一轮 `[...STORIES].reverse()` 是误判，已撤销。
- App 真相（读 MoonPage/moonpage-app）：书架顺序 = `data/stories/registry.ts` 的 `STORY_ORDER`，`bookNumber` = 该数组位置（1-based）；`app/library.tsx` 把 `VISIBLE_STORIES`（= slice(0,54)）原样交给 `WoodlandHome`，后者不反转不排序 → **b01 在最前（升序，最旧在前）**。App 有 54 本可见，文件 `bNN_*` 只是键、标题与文件名常不一致。
- 对齐方法：用脚本静态解析 App 全部故事（bookNumber+id+title），与网站 `lib/stories.ts` 的 45 篇**按标题匹配**。结果：45 篇全部命中（未匹配 0）。44 篇的 `file` 里 `bNN` 正好等于 App `bookNumber`；唯一错位是 **The Meadow Concert**——它是 App 第 53 本，却躺在网站第 31 位。
- 排序修正：把 `STORIES` 数组里 “The Meadow Concert” 移到**末尾**（对齐 App b53 位置）。其余 44 篇本就与 App 同序。`FEATURED_STORIES`（slice 0,3）仍为 b01–b03，未变。删除误加的 `LATEST_STORIES`，`/stories` 页面恢复用 `STORIES`。
- 封面修正：`public/covers` 里两个文件互相写反——meadow concert 的图挂名 `wp_b31_1_meadow_concert_cover.webp`、mouse looked 的图挂名 `wp_b53_1_mouse_looked_cover.webp`。按 App（`story-images.ts`：`WARM_PAINTERLY_B53.cover=wp_b53_1_meadow_concert_cover.webp`、`B31=wp_b31_1_mouse_looked_cover.webp`）对调两文件名；The Meadow Concert 的 `file` 改 `wp_b53_1_meadow_concert_cover.webp`；`lib/cover-versions.ts` 对应键同步改名（哈希不变）。
- 校验：`tsc --noEmit` 与 `eslint` 通过；重跑对齐脚本 → 未匹配 0、封面 bNN 不符 0。
- 复盘教训：网站 `lib/stories.ts` 是手工维护的 App 采样，已和 App 漂移；对齐 App 必须按**标题**而非 `slug`/`file` 的 `bNN` 硬猜。临时脚本在 `/tmp/app_order_check.mjs`。

## 故事库收敛为 App 严格前 45 本（同日下午，最终澄清）
- 用户最终澄清：「网站只显示前面45本，和app顺序、封面图一样」。上一段虽把顺序对齐 App，但数组仍含 App 第 53 本《The Meadow Concert》(当时占位在 webBNN=b31)、缺 App 第 31 本《The Moon's Light》——即「前 45 本」并非严格 App b01–b45。
- 修正 `lib/stories.ts` 的 `STORIES`：在 b30《The Little Lighthouse》后插入 App b31《The Moon's Light》(slug `the-moons-light`、file `wp_b31_1_mouse_looked_cover.webp`、tags animals/curiosity/magic/garden/night、含 house-style hook/bedtimeNote)；删除数组末尾的 App b53《The Meadow Concert》(slug `the-meadow-concert`、file `wp_b53_1_meadow_concert_cover.webp`)。数组现严格为 App b01–b45。
- 同步：重跑 `node scripts/sync-covers.mjs` → 同步 54 张封面、exit 0，自动再生 `lib/cover-versions.ts`（`wp_b31_1_mouse_looked_cover.webp` 键进、`wp_b53_1_meadow_concert_cover.webp` 键出）；`public/covers` 文件名此前已对调正确，无需再动。
- `/stories/[slug]` 静态路径由 `generateStaticParams()` 派生自 `STORIES.map(s => s.slug)`：新增 `the-moons-light` 页、旧 `the-meadow-concert` 页不再生成（build 清 `out/` 旧页）。`FEATURED_STORIES`(slice 0,3)= b01–b03 不变。
- 校验：`tsc --noEmit` 与 `eslint` 通过；`npm run build` exit 0；对齐脚本 `/tmp/app_order_check.mjs` → 未匹配 0 | 封面 bNN 不符 0；`cover-versions.ts` 含 b31 键、无 b53 键；`/stories/[slug]` 列 45 条路径（含新 `the-moons-light`）。
