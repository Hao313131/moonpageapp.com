# 2026-08-18

## GSC 索引编制告警排查与修复（moonpageapp.com）

用户贴了 Search Console 三类告警：网页会自动重定向 / 备用网页（有适当的规范标记）/ 未找到(404)。
站点为 Next.js 16 `output:"export"` + `trailingSlash:true`，部署到 GitHub Pages（自定义域名 moonpageapp.com）。

**根因（代码层，已修复）**：trailingSlash 下真实页面在 `/foo/`，canonical/sitemap 也带斜杠，但有两处对外暴露的 URL 用无斜杠写法，Google/Bing 顺着爬会 301 跳转到 `/foo/`，被记为"重定向"+"备用网页"：
1. `scripts/gen-feed.ts` 的 RSS `<link>/<guid>`（story/guide/collection）无斜杠 → 已加斜杠。
2. `lib/site.ts` 的 `hubJsonLd` BreadcrumbList/ItemList URL（`${SITE.domain}${path}`，无斜杠）→ 新增 `withSlash()` 统一补斜杠（注意：裸域名 `https://moonpageapp.com` 也会被 `.com` 误判为扩展名，需用"剥离 host 后再判路径段"的写法）。
3. `app/privacy-choices/page.tsx`、`app/support/page.tsx` 里的 `/privacy` 裸链接 → 改为 `/privacy/`。

**验证**：`npm run build` 后复跑全站扫描（/tmp/seo_check.mjs）→ sitemap 0 缺文件、站内 0 内容 404、0 个会 301 的无斜杠内链；构建产物 JSON-LD 与 feed.xml 均带斜杠。

**未修复（仓库外/外部因素）**：
- 404 来自外部：旧外链死 slug、GitHub Pages 项目地址 `user.github.io/moonpageapp.com`、www 子域（若 DNS 有 www 但 GH Pages 未配置 www）。`_redirects`/`_headers` 已写好但 GH Pages 忽略，需 Cloudflare 反代或迁 Cloudflare Pages/Netlify 才生效。
- 仍需 `git push` 触发 GitHub Pages 部署，再在 GSC 用"网址检查"请求重新编入索引。

**约定**：本仓库 EAS 规则不适用（那是 RN app）；GitHub Pages 静态构建免费、本地 `npm run build` 可作真实验证，但 push=部署由用户决定。

## GSC 全量明细第二轮（用户贴出 68/63/30/30/1 五类）

用户贴了 GSC 完整分类（每类只显示前 10 行，真实数量 68/63/1/30/1）。补充结论：

- **第1类 重定向(68)**：例子多为 `https://…/foo`（无斜杠）→ 已被 trailingSlash 修复系统性覆盖（共享代码，非逐 URL）；少量 `http://…/foo` → 外部 http→https。
- **第2类 备用网页(63)**：例子**全是 `http://…/foo/`**（HTTP+带斜杠）→ 纯 http→https 跳转，站点代码零 `http://` 引用（已验证 out/ 0 处），属外部发现，重爬后消退。
- **第3类 404(1)**：`https://moonpageapp.com/stories/milos-little-boat/`（带斜杠仍 404）→ 站内/数据/产物全搜不到，是外部/历史死 slug，非代码可修。
- **第4类 已发现未编入(30)**：例子 28/30 是真实集合页（已验证 built+in-sitemap，正常待收录）；**2 个不存在** `stories-about-honesty`、`stories-about-music`（不在 22 个真实 slug 里，旧 slug，Google 抓取后会变 404）。
- **第5类 已抓取未编入(1)**：`guides/toddler-who-wont-sit-still-for-books/`（真实页，内容薄/重复导致未收录）。

**新增修复/工具**：
1. `public/_redirects` 加了 3 条 legacy 301：`/stories/milos-little-boat`→`/stories/`、`/collections/stories-about-honesty`→`/collections/`、`/collections/stories-about-music`→`/collections/`（含斜杠/无斜杠两版）。⚠️ GH Pages 忽略 `_redirects`，需 Cloudflare 反代才生效。
2. 新增 `scripts/seo-classify-urls.mjs`：把 GSC 导出的全量 URL 列表喂进去，对照 `out/` 分类为 VALID/REDIRECT/404，自动列出所有死链及建议 301 目标。已用贴出的 31 条示例验证：9 VALID / 19 REDIRECT(9 无斜杠+10 http) / 3 404。
3. 全量死链只能靠 GSC 后台导出完整 URL（UI 每页仅 10 条）再跑分类脚本；目前仅能识别 user 贴出的 3 个。

**对用户的最终建议**：① commit+push 部署（trailingSlash 修复生效）；② 把 GSC 每类完整 URL 导出跑分类脚本，捞全所有死链补进 `_redirects`；③ Cloudflare 反代 GitHub Pages 以激活 `_redirects`/`_headers`（同时消 http/www 变体 + 缓存头）；④ GSC 请求重新编入索引。

## 第三轮：执行提交 + 必应/谷歌答疑（18:37）

用户要求"现在就把能做的全做了"并问必应/谷歌是否要重提交。

**已执行（本机可做）**：
1. 部署早已 commit+push 到 origin/master（环境自动提交，push "Everything up-to-date"），`deploy-pages.yml` 跑 `npm run build` 重建部署。✅
2. **必应 IndexNow 提交**：`node scripts/indexnow.mjs` → `OK (200) accepted, 149/149 URLs`。⚠️ 关键：deploy-pages.yml 第57行每次部署都自动跑 indexnow，所以必应**一直都在提交**（用户误以为没提交过）。手动再跑确认成功。
3. **谷歌/必应 sitemap ping**：谷歌 `google.com/ping?sitemap=` 返回 404、必应 `bing.com/ping?sitemap=` 返回 410 —— 两个旧端点都已被官方废弃（必应 410=已迁 IndexNow）。无需手动 ping。

**答疑结论**：
- 必应：已通过 IndexNow 自动提交（每次部署），无需额外操作。可在 Bing Webmaster Tools 确认。
- 谷歌：无 IndexNow 批量接口；GSC 里已提交的 sitemap.xml 会被 Google 定期自动重读，**无需重新提交**。若从未在 GSC 提交过 sitemap，才需一次性添加 `https://moonpageapp.com/sitemap.xml`。旧 ping 端点已废，别依赖。
- ⑤ 薄 guide 复查：读 `lib/guides.ts` 的 `toddler-who-wont-sit-still-for-books` —— 实际有 5 个充实小节+3 FAQ，**内容不薄**。未收录是新站大量相似 guide 下 Google 抓取预算/权重所致，会随权重增长自然收录，**无需改文案**。

**用户必须自己做的（外部/权限）**：
1. GitHub Actions 部署是否成功（我看不到 Actions 结果，需用户在 repo Actions 页确认绿）。
2. Cloudflare 反代 GitHub Pages + 建 Redirect Rules（让 `_redirects` 生效，③和④的2个死链才从404变301；同时消 http/www）。这是我给不出账号的部分——已准备确切规则（见回复）。
3. GSC「网址检查」对受影响 URL 请求重新编入索引（加速 ①② 下降）。

