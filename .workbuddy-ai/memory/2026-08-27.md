## 2026-08-27
- 修复 MoonPage `components/HubFaq.tsx`：为复用的 Hub FAQ 增加 `page-gutter`、居中最大宽度和响应式上下间距，并提升中等屏幕标题字号，解决 `/bedtime-stories` 与 `/bedtime-stories-by-age` 底部 FAQ 区域贴边、与上方布局不一致的问题。
- 运行 `npm run build` 成功：内容校验通过（45 stories、64 guides、22 collections），静态页面 156 个全部生成；已验证两个 hub 页面导出的 HTML 含新 FAQ 布局类。
- 优化 `components/Footer.tsx`：底部品牌区改为响应式网格；桌面端品牌 Logo 与 IG CTA 顶部对齐，移动端自然堆叠且 IG 区域右对齐；描述文案归入品牌列，避免 Logo/IG 垂直错位。再次构建成功并验证首页、hub、FAQ、搜索及 `/get` 静态 HTML 均包含统一 Footer 布局。
